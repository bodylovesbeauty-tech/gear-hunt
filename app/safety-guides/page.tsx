import { FeatureExperience } from '@/components/feature-experience'

export const metadata = { title: 'Navigation & Weather | BBBT', description: 'A clearly labelled route and weather preparation prototype for riders.', alternates: { canonical: '/safety-guides' } }

export default function SafetyGuidesPage() { return <FeatureExperience kind="navigation" /> }
