import { UnifiedSignup } from '@/components/unified-auth'
export const metadata={title:'Create BBBT Account',description:'Create one BBBT account for rider and approved role access.'}
export default function Page(){return <UnifiedSignup/>}
